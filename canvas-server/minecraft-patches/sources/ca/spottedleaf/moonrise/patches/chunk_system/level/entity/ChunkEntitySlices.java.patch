--- a/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/ChunkEntitySlices.java
+++ b/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/ChunkEntitySlices.java
@@ -198,6 +_,12 @@
         return this.entities.size() != 0;
     }
 
+    // Canvas start - getEntityArr
+    public Entity[] getEntityArr() {
+        return this.entities.getRawData();
+    }
+
+    // Canvas end - getEntityArr
     public List<Entity> getAllEntities() {
         final int len = this.entities.size();
         if (len == 0) {
@@ -400,7 +_,7 @@
 
     private static final class BasicEntityList<E extends Entity> {
 
-        private static final Entity[] EMPTY = new Entity[0];
+        private static final Entity[] EMPTY = io.canvasmc.canvas.util.ArrayConstants.emptyEntityArray; // Luminol - reduce array allocations
         private static final int DEFAULT_CAPACITY = 4;
 
         private E[] storage;
@@ -411,7 +_,7 @@
         }
 
         public BasicEntityList(final int cap) {
-            this.storage = (E[])(cap <= 0 ? EMPTY : new Entity[cap]);
+            this.storage = (E[])(cap <= 0 ? io.canvasmc.canvas.util.ArrayConstants.emptyEntityArray : new Entity[cap]); // Luminol - reduce array allocations
         }
 
         public boolean isEmpty() {
@@ -423,7 +_,7 @@
         }
 
         private void resize() {
-            if (this.storage == EMPTY) {
+            if (this.storage == io.canvasmc.canvas.util.ArrayConstants.emptyEntityArray) { // Luminol - reduce array allocations
                 this.storage = (E[])new Entity[DEFAULT_CAPACITY];
             } else {
                 this.storage = Arrays.copyOf(this.storage, this.storage.length * 2);
